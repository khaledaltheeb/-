import '../directory-scoped.css';
import '../profile-scoped-v3.css';
import '../location-map-scoped.css';

export default function DirectoryProfileLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
