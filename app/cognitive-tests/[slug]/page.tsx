import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import SiteHeader from '@/components/site-header';import SiteFooter from '@/components/site-footer';import ContentRenderer from '@/components/content-renderer';import CognitiveLabRunner from '@/components/cognitive-lab-runner';
import {getCognitiveTool} from '@/lib/cognitive-lab/catalog';import {currentCognitiveSlug} from '@/lib/historical-cognitive-tests';import {getLegacyPreservedPage,legacyPreservedMetadata} from '@/lib/legacy-preserved-page';
export const dynamic='force-dynamic';type Params=Promise<{slug:string}>;const routeFor=(slug:string)=>`/cognitive-tests/${slug}/`;
export async function generateMetadata({params}:{params:Params}):Promise<Metadata>{const {slug}=await params,route=routeFor(slug);return legacyPreservedMetadata(await getLegacyPreservedPage(route),route);}
export default async function HistoricalCognitiveTestPage({params}:{params:Params}){const {slug}=await params;const mapped=currentCognitiveSlug(slug);if(!mapped)notFound();const tool=getCognitiveTool(mapped);if(!tool)notFound();const route=routeFor(slug),page=await getLegacyPreservedPage(route);if(!page)notFound();return <><SiteHeader/><main className="cognitive-lab-page cognitive-tool-page"><section className="cognitive-tool-hero"><div className="cognitive-shell"><span className="cognitive-kicker">نسخة المسار التاريخي · تعليمي غير تشخيصي</span><h1>{page.h1||page.title||tool.title}</h1><p>{page.meta_description||tool.summary}</p><div className="cognitive-tool-badges"><span>المهمة الفعلية مفعّلة</span><span>لا توجد درجة ذكاء</span><span>لا يوجد تشخيص</span></div></div></section><div className="cognitive-shell cognitive-runner-wrap"><CognitiveLabRunner tool={tool}/></div><section className="cognitive-shell cognitive-reading"><div className="article-body"><ContentRenderer bodyJson={page.body_json} bodyText={page.body_text} recordId={page.source_path}/></div></section></main><SiteFooter/></>;}