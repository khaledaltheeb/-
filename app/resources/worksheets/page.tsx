import type { Metadata } from 'next';import Link from 'next/link';import SiteHeader from '@/components/site-header';import SiteFooter from '@/components/site-footer';import { worksheets,resourceSafetyNote } from '@/lib/practical-resources';import '../practical-resources.css';
export const metadata:Metadata={title:'أوراق عمل الصحة النفسية والدعم',description:'خمس أوراق عمل قابلة للطباعة لتنظيم الملاحظة والأسئلة والدعم الأسري والصفي والدراسي ومراجعة المحتوى، دون درجات أو تشخيص.',alternates:{canonical:'/resources/worksheets'},robots:{index:true,follow:true}};
export default function WorksheetsPage(){return <><SiteHeader/><main className="resource-page"><section className="resource-hero"><div className="resource-shell"><span className="resource-eyebrow">النماذج الأصلية منقولة وظيفيًا</span><h1>أوراق عمل الصحة النفسية والدعم</h1><p>استعدنا الأسئلة الأصلية من مصدرها البرمجي، وأعدنا بناء النماذج بحيث تعمل على الهاتف والطباعة من دون حفظ تلقائي للإجابات.</p></div></section><section className="resource-shell resource-section"><div className="resource-grid">{worksheets.map((item)=><Link className="resource-card" href={`/resources/worksheets/${item.slug}`} key={item.slug}><span className="resource-eyebrow">{item.audience}</span><h2>{item.title}</h2><p>{item.purpose}</p><span className="resource-link">فتح الورقة</span></Link>)}</div><div className="resource-safety">{resourceSafetyNote}</div></section></main><SiteFooter/></>}
