import type { Metadata } from 'next';import { notFound } from 'next/navigation';import SiteHeader from '@/components/site-header';import SiteFooter from '@/components/site-footer';import WorksheetForm from '@/components/worksheet-form';import { findWorksheet,worksheets,resourceSafetyNote } from '@/lib/practical-resources';import '../../practical-resources.css';
type Params=Promise<{slug:string}>;export const dynamicParams=false;export function generateStaticParams(){return worksheets.map((item)=>({slug:item.slug}))}
export async function generateMetadata({params}:{params:Params}):Promise<Metadata>{const {slug}=await params;const item=findWorksheet(slug);if(!item)return{};return{title:item.title,description:item.purpose,alternates:{canonical:`/resources/worksheets/${slug}`},robots:{index:false,follow:true,noarchive:true}}}
export default async function WorksheetPage({params}:{params:Params}){const {slug}=await params;const item=findWorksheet(slug);if(!item)notFound();return <><SiteHeader/><main className="resource-page"><section className="resource-hero"><div className="resource-shell"><span className="resource-eyebrow">{item.audience} · ورقة عمل غير تشخيصية</span><h1>{item.title}</h1><p>{item.purpose}</p></div></section><section className="resource-shell resource-section"><WorksheetForm title={item.title} prompts={item.prompts}/><div className="resource-safety">{resourceSafetyNote} لا تكتب اسمًا كاملًا أو رقم هوية أو معلومات لا ترغب في ظهورها على النسخة المطبوعة.</div></section></main><SiteFooter/></>}
