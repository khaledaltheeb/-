import type { Metadata } from 'next';import Link from 'next/link';import SiteHeader from '@/components/site-header';import SiteFooter from '@/components/site-footer';import { infographics,resourceSafetyNote } from '@/lib/practical-resources';import '../practical-resources.css';
export const metadata:Metadata={title:'إنفوجرافيك الصحة النفسية',description:'بطاقات تثقيفية نصية عالية التباين وقابلة للطباعة حول حدود التشخيص، دعم السلوك وفحص موثوقية المحتوى النفسي.',alternates:{canonical:'/resources/infographics'},robots:{index:true,follow:true}};
export default function InfographicsPage(){return <><SiteHeader/><main className="resource-page"><section className="resource-hero"><div className="resource-shell"><span className="resource-eyebrow">مواد بصرية نصية منقولة من المستودع القديم</span><h1>إنفوجرافيك الصحة النفسية</h1><p>استعدنا المواد الثلاث على مساراتها الأصلية كصفحات عالية التباين تعمل على الهاتف والطباعة ولا تعتمد على صورة غير قابلة للقراءة بقارئ الشاشة.</p></div></section><section className="resource-shell resource-section"><div className="resource-grid">{infographics.map((item)=><Link className="resource-card" href={`/resources/infographics/${item.slug}`} key={item.slug}><h2>{item.title}</h2><p>{item.summary}</p><span className="resource-link">فتح المادة</span></Link>)}</div><div className="resource-safety">{resourceSafetyNote}</div></section></main><SiteFooter/></>}
