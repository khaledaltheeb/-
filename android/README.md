# Rawafid Life OS — Android

هذه ليست نسخة WebView من الموقع. التطبيق طبقة حياة يومية فوق `healthrenewal.org`؛ الموقع يبقى محرك المعرفة، بينما تعمل أدوات الهاتف محليًا قدر الإمكان.

## الدفعة الحالية

- واجهة Native متعددة الأقسام: الرئيسية، اهتم بي، الأدوات، الأمان، المعرفة.
- Light/Dark design system عالي التباين وRTL.
- تذكير اختياري «ارمش الآن» لراحة العين.
- تذكير الحركة والتنفس بعد الجلوس.
- تذكير الماء بتردد يختاره المستخدم.
- رسائل تحفيزية: التردد والحد اليومي بيد المستخدم.
- ساعات صمت تمنع التذكيرات المزعجة.
- مواعيد علاج محلية عبر AlarmManager، مع إعادة الجدولة بعد إعادة تشغيل الهاتف.
- «فضفض ثم اتركها»: النص لا يُكتب إلى SharedPreferences أو خادم، ويزول من الذاكرة عند ترك الشاشة.
- عداد ماء يومي محلي.
- بطاقة طوارئ محلية قابلة للمشاركة كنص.
- جواز احتياجاتي Support Passport محلي: المستخدم يحدد احتياجات التواصل والوصولية، ويحدد لكل بند إن كان يظهر عند المشاركة أم لا.
- موقع روافد داخل WebView مقيد بنطاق `healthrenewal.org`، والروابط الخارجية تخرج إلى المتصفح.
- لا تعتمد الأدوات اليومية أو جواز الاحتياجات على Supabase أو Cloudflare.

## قرار بنك رفيقة روافد 12K

توسعة بنك رفيقة روافد إلى 12,000 رسالة مؤجلة عمدًا إلى المرحلة النهائية من المنتج. الملفات الموجودة حاليًا لا تُحذف ولا تُستبدل، لكن اكتمال 12K ليس شرطًا لبناء Android أو لاستكمال الميزات الأساسية الآن. فحص اكتمال البنك أصبح Workflow يدويًا يستخدم فقط عند بدء مرحلة رفع المحتوى النهائية.

## الخصوصية

التفضيلات والمواعيد والبطاقات وجواز الاحتياجات تحفظ في مساحة التطبيق المحلية في هذه المرحلة. `allowBackup=false` لمنع نسخ هذه البيانات ضمن النسخ الاحتياطي التقليدي للتطبيق. «فضفض» لا تُحفظ أصلًا.

## البناء

```bash
gradle -p android :app:assembleDebug :app:testDebugUnitTest
```

GitHub Actions يبني APK عند فتح Pull Request يمس `android/**`.
