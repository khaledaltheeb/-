# Addiction Atlas continuity contract

Target repository: `khaledaltheeb/-`.

## Non-regression floor
The historical migrated floor remains important for detecting destructive regressions:
- At least 54 substance/family records.
- At least 34 editorial, indexable comparison pages.
- At least 6 explicitly reviewed interaction pairs; absence from the matrix never means a combination is safe.
- 8 independent risk dimensions: acute toxicity, overdose, dependence, medical withdrawal risk, neuro harm, cardio harm, respiratory harm, polysubstance risk.
- At least 18 substances with claim-level/axis-level evidence tracing, covering all 8 dimensions for each reviewed substance (144 evidence cells at the 2026-08-26 checkpoint).
- Every reviewed evidence cell retains the published score, an independent A/B/C/U evidence grade, exposure/context text, rationale text and one or more resolvable source IDs.
- A `U` evidence grade must keep the risk value `null`; uncertainty must never be converted into a guessed number.
- At least 7 curated epidemiology records and 6 mortality records, each retaining year, geography, metric definition and a resolvable source ID.
- Arabic + English display names on every substance page.
- Arabic transliterations, aliases and misspellings are for search resolution/metadata only; never publish them as hidden keyword stuffing.
- No use-dose instructions, mixing recipes, intoxication optimization, or self-managed dangerous withdrawal schedules.
- Every substance record keeps direct source URLs and evidence grade A/B/C/U.
- Population statistics must never be converted into personal risk probabilities, and category-level numbers must never be assigned to an individual substance without an explicit source-level mapping.
- Interaction evidence must distinguish direct-pair evidence from class-to-substance or class-to-class evidence. Never describe a class warning as if a pair-specific clinical trial established it.
- No existing published page may be deleted, hidden or converted to `noindex` as part of atlas work.

## Current production baseline before wave 7
Wave 6 is the currently proven production baseline before the wave 7 release candidate:
- 57 substance/family records.
- At least 34 curated/indexable comparisons.
- At least 8 reviewed interaction records.
- 21 substances with axis-level evidence = 168 evidence cells.
- The wave 6 extension includes `tianeptine`, `carfentanil`, and `medetomidine`.
- Production deployment and the independent production verifier both succeeded after wave 6.

Future work must preserve this production baseline even if the historical source snapshot or migration tooling still contains older counts.

## Wave 7 release candidate — 2026-08-27
Wave 7 is an additive, source-grounded release candidate. It must not be described as production until deployment and independent live verification both succeed.

Target release contract:
- At least 71 substance/family records.
- At least 37 curated/indexable comparisons; no combinatorial page factory.
- At least 11 reviewed interaction records.
- At least 35 substances with axis-level evidence = 280 evidence cells.
- At least 9 curated epidemiology/surveillance records and 8 mortality records.
- 14 wave 7 substances: `7-hydroxymitragynine`, `phenibut`, `hexahydrocannabinol-hhc`, `carisoprodol`, `protonitazepyne`, `metonitazepyne`, `etonitazepipne`, `n-desethyl-isotonitazene`, `3-oh-pcp`, `n-ethylheptedrone`, `isotonitazepyne`, `n-desethyl-etonitazene`, `mdmb-fubinaca`, and `cychlorphine`.
- Three new selective comparisons only: `kratom-vs-7-oh`, `cannabis-vs-hhc`, and `phenibut-vs-gabapentin`.
- Three reviewed carisoprodol interactions: fentanyl, diazepam, and alcohol. Missing pairs remain “not reviewed yet”, never “safe”.
- Phenibut poison-center data are surveillance reports, not prevalence estimates or personal probabilities. The 1320 exposure reports and 12.6% major-effects share retain the 2009–2019 cumulative context; the three reported deaths are not presented as national mortality.
- `cychlorphine` is an early-warning substance with an EUDA initial report. All eight numeric risk axes remain `null` and all axis evidence grades remain `U` until evidence supports responsible scoring. Classification as an emerging opioid must not be used by itself to manufacture numeric risk values.

## Source snapshot and extension ownership
The original migrated scientific dataset is pinned to historical source repository commit `00014486191027349cc083e824e545da186d74d1`. Runtime code serves local data in `data/addiction-atlas/`; it must not fetch the old repository at request time and must never read a moving source `main` branch.

The historical vendoring script may refresh only the pinned legacy files whose hashes belong to that source snapshot. The extension manifests `substance-waves.json` and `risk-evidence-manifest.json` are repository-owned and MUST NOT be overwritten by the legacy vendoring script. Post-snapshot wave files, source registries, interactions, surveillance records and comparison extensions are repository-owned additive layers.

A vendor refresh must preserve all extension layers together so that no score, interaction, numerical record, comparison or route becomes detached from its source metadata. `scripts/addiction-atlas-contract.mjs` enforces that the vendoring script cannot overwrite the two extension manifests.

## Required publication surfaces
- `/addiction/` as the discoverable institutional gateway while preserving the pre-existing editorial hub content.
- `/sectors/addiction-recovery/` must expose the atlas product and its primary routes.
- `/addiction/substances/`
- `/addiction/substances/[slug]/`
- `/addiction/compare/` with an interactive two-substance explorer plus reviewed editorial comparison pages.
- `/addiction/compare/[slug]/`
- `/addiction/interactions/`
- `/addiction/prevalence/`
- `/addiction/mortality/`
- `/addiction/methodology/`
- `/sitemaps/addiction-atlas.xml`

The root sitemap index must include the atlas sitemap. The addiction sector navigation must expose the atlas, comparisons, reviewed interactions, population evidence surfaces and methodology. Comparison and evidence pages must remain printable in an RTL A4 layout without turning a lower score on one axis into a blanket safety claim.

## Scientific display rules
- Substance pages with axis-level review must expose the evidence behind each reviewed risk dimension, including evidence grade, context, rationale and named source links.
- Substance pages without axis-level review must say so explicitly; they must not silently inherit a stronger confidence label.
- The overall substance evidence grade is not a substitute for axis-level evidence. Different dimensions may have materially different certainty.
- Scores are ordinal within a dimension. Do not sum or average the eight dimensions into a total danger score.
- The interaction layer is a reviewed evidence registry, not a mixing checker. It must never generate unreviewed pair conclusions algorithmically.
- The interactive comparison explorer may compare published axis values for any two substances, but it must never auto-generate an editorial conclusion for a pair that lacks a reviewed comparison page.
- A regulatory scheduling decision proves regulatory/public-health concern in its stated context; it must not be transformed into a precise potency ratio, personal probability, or unmeasured organ-risk score.
- Class-level opioid evidence may support clearly labeled class inference (for example respiratory depression or CNS-depressant mixing risk), but it must not be presented as pair-specific evidence.

## Validation checkpoint — 2026-08-26
The evidence-surface upgrade merged through PR #396. The interaction upgrade merged through PR #399. Each later axis-evidence release must preserve the proven production floor and pass `npm run addiction-atlas:validate` plus repository TypeScript/lint/build validation. Production must not be described as updated until its own deployment and live checks have completed successfully.

## Production release closure — wave 6, 2026-08-27
The institutional product layer was completed on `main`: the existing `/addiction/` editorial content was preserved and augmented with a discoverable interactive atlas gateway; the `addiction-recovery` sector exposes the atlas routes; `/addiction/compare/` includes a true two-substance interactive explorer while retaining curated editorial comparison pages; and `scripts/addiction-atlas-product-contract.mjs` prevents regression of discovery, interaction safety, and anti-scaled-content rules.

An initial release attempt exposed a Cloudflare Worker size regression caused by the bundled `next/og` PWA icon renderer. The implementation was corrected to serve pre-rendered PNG bytes without pulling the heavy image-rendering WASM into the Worker. The subsequent wave 6 production deployment succeeded, including scientific/product validation, the Cloudflare Worker build, deployment, live release checks, and the independent `Verify Addiction Atlas Production` workflow.

This closure remains evidence that wave 6 was production-proven; it is not evidence that later waves are live. Every later atlas release must again pass production deployment and independent live verification.
