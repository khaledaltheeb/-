name: Wire Rawafid Reader Toolbar Once
on:
  push:
    branches: ["android/rawafid-app-v1"]
    paths:
      - ".github/workflows/android-wire-reader-toolbar-once.yml"
permissions:
  contents: write
jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: android/rawafid-app-v1
          fetch-depth: 0
      - name: Patch reader toolbar
        run: python3 scripts/patch_android_reader_toolbar.py
      - name: Commit reader toolbar
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add android-app/app/src/main/java/org/healthrenewal/rawafid/MainActivity.java
          git commit -m "feat(android): add library and share reader toolbar"
          git push origin HEAD:android/rawafid-app-v1
