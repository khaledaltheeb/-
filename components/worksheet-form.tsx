'use client';
import { useState } from 'react';import PrintResourceButton from '@/components/print-resource-button';
export default function WorksheetForm({title,prompts}:{title:string;prompts:readonly string[]}){const [answers,setAnswers]=useState<string[]>(()=>prompts.map(()=>''));return <section className="worksheet-form"><div className="worksheet-actions"><PrintResourceButton/><button type="button" onClick={()=>setAnswers(prompts.map(()=>''))}>مسح الإجابات</button></div><div className="worksheet-privacy"><strong>الخصوصية:</strong> ما تكتبه يبقى في ذاكرة الصفحة الحالية فقط. لا حفظ تلقائي ولا إرسال للخادم. إذا أردت الاحتفاظ به استخدم الطباعة أو الحفظ كملف PDF على جهازك.</div>{prompts.map((prompt,index)=><label className="worksheet-question" key={prompt}><span>{index+1}. {prompt}</span><textarea rows={5} maxLength={1600} aria-label={`${title}: ${prompt}`} value={answers[index]} onChange={(e)=>setAnswers((current)=>current.map((value,i)=>i===index?e.target.value:value))}/></label>)}</section>}
