const base=process.env.SMOKE_BASE_URL||'http://127.0.0.1:3000';
const publicRoutes=['/','/theme-preview','/search','/sectors','/sections','/specialists','/centers','/community','/about','/join','/join/specialist','/join/center','/login','/register','/forgot-password','/reset-password','/offline','/share','/cognitive-lab','/cognitive-lab/working-memory-updating','/cognitive-lab/prospective-memory','/cognitive-lab/associative-binding','/sitemaps/cognitive-lab.xml','/manifest.webmanifest','/sw.js','/robots.txt','/pwa-icon-180','/pwa-icon-192','/pwa-icon-512','/api/health'];
const protectedRoutes=['/dashboard','/messages','/appointments','/notifications','/account','/account/security','/account/verification-documents','/admin','/specialist','/specialist/content','/specialist/media','/center'];
const legacyLabRedirects=[['/cognitive-lab/associative-context-binding','/cognitive-lab/associative-binding'],['/cognitive-lab/prospective-memory-cues','/cognitive-lab/prospective-memory']];
let failed=false;
for(const route of publicRoutes){try{const response=await fetch(`${base}${route}`,{redirect:'manual'});if(response.status<200||response.status>=400){console.error(`PUBLIC ${route}: ${response.status}`);failed=true;}else if(route.startsWith('/pwa-icon-')&&!String(response.headers.get('content-type')||'').includes('image/png')){console.error(`PUBLIC ${route}: expected image/png`);failed=true;}else if(route==='/api/health'){const payload=await response.json().catch(()=>null);if(!payload||payload.status!=='ok'||payload.supabase!=='ok'||payload.database!=='ok'){console.error('HEALTH: application or Supabase unhealthy',payload);failed=true;}else console.log('HEALTH: application + Supabase ok');}else if(route==='/sitemaps/cognitive-lab.xml'){const body=await response.text();const urls=(body.match(/<url>/g)||[]).length;if(!body.includes('/cognitive-lab/working-memory-updating')||!body.includes('/cognitive-lab/prospective-memory')||!body.includes('/cognitive-lab/associative-binding')||urls!==54){console.error(`COGNITIVE_SITEMAP: expected 54 canonical URLs, got ${urls}`);failed=true;}else console.log('COGNITIVE_SITEMAP: 54 canonical URLs verified');}else console.log(`PUBLIC ${route}: ${response.status}`);}catch(error){console.error(`PUBLIC ${route}:`,error);failed=true;}}
for(const [route,target] of legacyLabRedirects){try{const response=await fetch(`${base}${route}`,{redirect:'manual'});const location=response.headers.get('location')||'';if(![301,302,307,308].includes(response.status)||!location.endsWith(target)){console.error(`LEGACY_LAB ${route}: expected redirect to ${target}, got ${response.status} ${location}`);failed=true;}else console.log(`LEGACY_LAB ${route}: ${response.status} -> ${target}`);}catch(error){console.error(`LEGACY_LAB ${route}:`,error);failed=true;}}
for(const route of protectedRoutes){try{const response=await fetch(`${base}${route}`,{redirect:'manual'});const location=response.headers.get('location')||'';if(![301,302,303,307,308].includes(response.status)||!location.includes('/login')){console.error(`PROTECTED ${route}: expected login redirect, got ${response.status} ${location}`);failed=true;}else console.log(`PROTECTED ${route}: ${response.status} -> login`);}catch(error){console.error(`PROTECTED ${route}:`,error);failed=true;}}
try{
  const response=await fetch(`${base}/__rawafid_missing_route_for_smoke__`,{redirect:'manual'});
  const body=await response.text();
  if(response.status!==404||!body.includes('الصفحة غير موجودة')){console.error(`NOT_FOUND: expected branded 404, got ${response.status}`);failed=true;}else console.log('NOT_FOUND: branded 404 verified');
}catch(error){console.error('NOT_FOUND:',error);failed=true;}
if(failed)process.exit(1);console.log('Rawafid production HTTP smoke checks passed.');
