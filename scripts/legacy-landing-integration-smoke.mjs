const base=process.env.SMOKE_BASE_URL||'http://127.0.0.1:3000';
const routes=['/about/','/accessibility-statement/','/accessibility/','/assessments/','/assessments/gad-7/','/audiences/','/capabilities/expanded/','/categories/الدافعية-والسلوك/','/cochrane/evidence-academy/','/cochrane/','/cognitive-tests/','/cognitive-tests/digit-span/','/daily-tools/','/daily-tools/active-listening-checklist/','/daily-tools/sleep-wind-down-plan/','/en/','/encyclopedia/all/','/encyclopedia/','/es/','/family/','/','/iris/cited-guides/','/iris/','/learning-paths/','/library/','/magazine/','/outside-the-box/','/quick-info/','/schools/','/sections/','/sectors/all-pages/','/sectors/calendars/','/sectors/home/','/sectors/','/sectors/women/','/sectors/youth/','/services/','/source-registry/','/specialists-partners/','/start-here/','/terms/','/tips/','/trust/','/verified-resources/'];
const mustRemainNoindex=new Set(['/assessments/','/assessments/gad-7/','/cognitive-tests/','/cognitive-tests/digit-span/','/daily-tools/','/daily-tools/active-listening-checklist/','/daily-tools/sleep-wind-down-plan/']);
const hasRobotsNoindex=(body)=>/<meta\b(?=[^>]*\bname=["']robots["'])(?=[^>]*\bcontent=["'][^"']*\bnoindex\b[^"']*["'])[^>]*>/i.test(body);
let failed=false;
for(const route of routes){try{const response=await fetch(`${base}${encodeURI(route)}`,{redirect:'manual'});const location=response.headers.get('location')||'';const body=await response.text();if(response.status!==200||location){console.error(`LANDING ${route}: expected in-place 200, got ${response.status} ${location}`);failed=true;continue;}if(body.replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim().length<180){console.error(`LANDING ${route}: response is too shallow`);failed=true;continue;}if(mustRemainNoindex.has(route)&&!hasRobotsNoindex(body)){console.error(`LANDING ${route}: preserved interactive route must remain noindex`);failed=true;continue;}console.log(`LANDING ${route}: 200 in place${mustRemainNoindex.has(route)?' + noindex':''}`);}catch(error){console.error(`LANDING ${route}:`,error);failed=true;}}
if(failed)process.exit(1);console.log(`Legacy landing integration smoke passed: ${routes.length} substantial landings resolve in place without redirects; preserved interactive routes remain noindex.`);